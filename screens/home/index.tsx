import HomeWrapper from "@/components/home-wrapper";
import SafeAreaWrapper from "@/components/safe-area-wrapper";
import { Text } from "react-native";

const Home = () => {
  return <HomeWrapper page="Home" />;
};

export default Home;
